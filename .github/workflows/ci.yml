name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
  workflow_call:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: ghcr.io/${{ github.repository }}-backend
  WEB_IMAGE: ghcr.io/${{ github.repository }}-web
  VOICE_IMAGE: ghcr.io/${{ github.repository }}-voice

jobs:
  test-backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg15
        env:
          POSTGRES_USER: collective
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: collective_will_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U collective"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
          enable-cache: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Backend parity checks
        env:
          DATABASE_URL: postgresql+asyncpg://collective:testpass@localhost:5432/collective_will_test
          TEST_DATABASE_URL: postgresql+asyncpg://collective:testpass@localhost:5432/collective_will_test
          ENVIRONMENT: test
          GENERATE_PIPELINE_CACHE: "0"
          CI_PARITY: "1"
        run: bash scripts/ci-backend.sh

  test-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Web parity checks
        run: bash scripts/ci-web.sh

  test-voice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Voice parity checks
        run: bash scripts/ci-voice.sh

  build-backend:
    needs: [test-backend]
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-web:
    needs: [test-web]
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build and push web
        uses: docker/build-push-action@v6
        with:
          context: ./web
          push: true
          build-args: |
            NEXT_PUBLIC_API_BASE_URL=/api
          tags: |
            ${{ env.WEB_IMAGE }}:latest
            ${{ env.WEB_IMAGE }}:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-voice:
    needs: [test-voice]
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Build voice-service image
        uses: docker/build-push-action@v6
        with:
          context: ./voice-service
          load: true
          tags: ${{ env.VOICE_IMAGE }}:ci-test
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test voice-service image
        run: |
          echo "==> Testing numpy/torch imports..."
          docker run --rm ${{ env.VOICE_IMAGE }}:ci-test \
            python -c "import numpy; import torch; import torchaudio; print(f'numpy={numpy.__version__} torch={torch.__version__}')"
          echo "==> Testing app module imports..."
          docker run --rm ${{ env.VOICE_IMAGE }}:ci-test \
            python -c "from app.embed import load_model; from app.transcribe import load_model; print('app imports ok')"

      - name: Push voice-service image
        uses: docker/build-push-action@v6
        with:
          context: ./voice-service
          push: true
          tags: |
            ${{ env.VOICE_IMAGE }}:latest
            ${{ env.VOICE_IMAGE }}:sha-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
